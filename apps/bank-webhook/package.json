{
  "name": "@paytm-wallet/bank-webhook",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start"
  },
  "license": "MIT",
  "dependencies": {
    "@paytm-wallet/database": "*",
    "esbuild": "^0.24.0",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0"
  }
}
